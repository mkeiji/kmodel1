name: Deployment

on:
    workflow_run:
        workflows:
            - Build
        types:
            - completed

jobs:
    deploy:
        uses: mkeiji/kcicd/.github/workflows/cd-deploy.yml@master
        with:
            command_timeout: "20m"
        secrets: inherit
